{
  "id": "performance-quic",
  "title": "Performance, Congestion & QUIC",
  "order": 14,
  "lessons": [
    {
      "id": "performance-quic-l1",
      "title": "The Performance Quantities: Bandwidth, Throughput, Latency, Jitter & Loss",
      "order": 1,
      "difficulty": 1,
      "content": "Before you can reason about a network, separate five quantities that beginners blur together.\n\n**Bandwidth** is *capacity* — the maximum bits/second a link can carry, e.g. a 1 Gbps interface. It is a property of the pipe, not of your transfer. **Throughput** is what you actually *achieve* end-to-end, always ≤ bandwidth because of contention, protocol limits, and overhead. **Goodput** is narrower still: the application payload bits delivered per second, stripping out headers, retransmissions, and handshake bytes. A link can run at line-rate throughput while goodput sags under heavy header or retransmit overhead.\n\n**Latency** is delay. Two flavors matter: *one-way delay* and *round-trip time* (RTT), the there-and-back time a ping measures. Most interactive performance is governed by RTT, not bandwidth — a fact that surprises people who think \"faster internet\" means \"more Mbps.\" Loading a page that needs many round trips is RTT-bound; a bulk download is bandwidth-bound.\n\n**Jitter** is the *variation* in latency packet-to-packet. A VoIP or video call tolerates moderate latency but hates jitter, because the playout buffer must absorb the variance or you get dropouts. This is exactly the timing-stability intuition from radar PRF and clock recovery: a stable interval is worth more than a fast-but-erratic one.\n\n**Loss** is the fraction of packets that never arrive (drops from full queues, bit errors, or congestion). Even tiny loss devastates throughput on long paths because reliable transports interpret loss as a congestion signal and back off. Hold these five apart and most \"the network is slow\" arguments resolve into a specific, measurable cause.",
      "items": [
        {
          "id": "performance-quic-l1-fc1",
          "type": "flashcard",
          "front": "Distinguish bandwidth from throughput.",
          "back": "Bandwidth is the link's maximum capacity (bits/s, a property of the pipe). Throughput is what you actually achieve end-to-end, always ≤ bandwidth due to contention, overhead, and protocol limits.",
          "hint": "Pipe size vs water actually flowing."
        },
        {
          "id": "performance-quic-l1-fc2",
          "type": "flashcard",
          "front": "What is goodput and how does it differ from throughput?",
          "back": "Goodput is the application payload bits/s delivered, excluding headers, retransmissions, and handshake bytes. Throughput counts all bits on the wire; goodput counts only useful ones."
        },
        {
          "id": "performance-quic-l1-fc3",
          "type": "flashcard",
          "front": "Why is much interactive performance RTT-bound rather than bandwidth-bound?",
          "back": "Work that needs many sequential round trips (DNS, handshakes, dependent requests) is paced by RTT, not Mbps. Adding bandwidth doesn't help if you're waiting on round trips.",
          "hint": "Think page load with many small dependent requests."
        },
        {
          "id": "performance-quic-l1-fc4",
          "type": "flashcard",
          "front": "Define jitter and name a workload that is especially sensitive to it.",
          "back": "Jitter is the variation in latency from packet to packet. Real-time audio/video (VoIP, conferencing) is sensitive: the playout buffer must absorb the variance or audio drops out."
        },
        {
          "id": "performance-quic-l1-fc5",
          "type": "flashcard",
          "front": "Why does even small packet loss hurt throughput badly on long paths?",
          "back": "Reliable transports treat loss as a congestion signal and reduce their sending rate (back off). On a high-RTT path the recovery and window re-growth take many round trips, so a tiny loss rate caps achievable throughput."
        },
        {
          "id": "performance-quic-l1-mc1",
          "type": "mcq",
          "prompt": "A 1 Gbps link consistently delivers 600 Mbps of HTTP payload during a bulk download. Which statement is most accurate?",
          "choices": [
            "Bandwidth is 600 Mbps",
            "Goodput is about 600 Mbps; bandwidth is 1 Gbps",
            "Throughput equals bandwidth here",
            "Latency must be 600 ms"
          ],
          "answerIndex": 1,
          "explanation": "Bandwidth (capacity) is 1 Gbps. The 600 Mbps of delivered payload is goodput, which sits below bandwidth because of overhead, contention, or window limits."
        },
        {
          "id": "performance-quic-l1-mc2",
          "type": "mcq",
          "prompt": "Which quantity does a standard ping primarily measure?",
          "choices": [
            "Bandwidth",
            "Goodput",
            "Round-trip time (RTT)",
            "Loss correction overhead"
          ],
          "answerIndex": 2,
          "explanation": "Ping sends an ICMP echo and times the reply, giving round-trip time. It can also report loss, but its headline number is RTT."
        },
        {
          "id": "performance-quic-l1-mc3",
          "type": "mcq",
          "prompt": "A video call has acceptable average latency but stutters badly. The most likely culprit is:",
          "choices": [
            "High jitter overwhelming the playout buffer",
            "Excess bandwidth",
            "Goodput exceeding throughput",
            "Too low an RTT"
          ],
          "answerIndex": 0,
          "explanation": "Stutter with acceptable average latency points to jitter — latency variation the playout buffer cannot smooth out."
        },
        {
          "id": "performance-quic-l1-mc4",
          "type": "mcq",
          "prompt": "Which is the best one-line description of the relationship among the metrics?",
          "choices": [
            "goodput ≥ throughput ≥ bandwidth",
            "bandwidth ≥ throughput ≥ goodput",
            "latency ≥ bandwidth ≥ jitter",
            "throughput ≥ bandwidth ≥ goodput"
          ],
          "answerIndex": 1,
          "explanation": "Capacity (bandwidth) bounds achieved throughput, which in turn bounds useful payload (goodput): bandwidth ≥ throughput ≥ goodput."
        }
      ]
    },
    {
      "id": "performance-quic-l2",
      "title": "Delay, Queues & the Bandwidth-Delay Product",
      "order": 2,
      "difficulty": 2,
      "content": "Total latency is a *sum of four delays* at every hop. **Processing delay**: the router/host examines headers and makes a forwarding decision (usually tiny). **Queuing delay**: time spent waiting in a buffer behind other packets — the variable, load-dependent term that explodes under congestion. **Serialization (transmission) delay**: time to clock the packet's bits onto the link, $d_{serial} = L / R$ for $L$ bits at rate $R$. **Propagation delay**: time for the signal to travel the distance, $d_{prop} = d / v$, where in fiber $v \\approx \\tfrac{2}{3}c \\approx 2\\times10^8\\,\\text{m/s}$. That two-thirds-of-light figure is your radar/RF timing intuition reused: a packet to a server 3000 km away eats at least $\\approx 15\\,\\text{ms}$ one-way of pure propagation, before any queuing.\n\n**Little's Law** gives queue intuition: $L = \\lambda W$ — the average number of items in a system equals arrival rate times average time in system. Rearranged, if a queue holds many packets at a given drain rate, the time-in-queue (latency) is large. You cannot fix a persistently overfull queue by waiting; you must drop or slow arrivals.\n\nThe headline tool is the **bandwidth-delay product**: $BDP = BW \\times RTT$. It is the amount of data \"in flight\" filling the pipe end-to-end — the volume of a cylinder whose cross-section is bandwidth and length is RTT. To keep a link *fully utilized*, the sender's in-flight window must be at least the BDP; otherwise it sends a burst, then idles waiting for ACKs. This is why **TCP window scaling** (RFC 7323) exists: the original 16-bit window maxes at 64 KiB, far too small for a \"long fat network.\" On a 1 Gbps link with 80 ms RTT, $BDP = 10^9 \\times 0.08 / 8 \\approx 10\\,\\text{MB}$ — you need a ~10 MB window, not 64 KiB, to fill it.",
      "items": [
        {
          "id": "performance-quic-l2-fc1",
          "type": "flashcard",
          "front": "Name the four components of per-hop delay.",
          "back": "Processing delay (header inspection/forwarding), queuing delay (waiting in buffer), serialization/transmission delay (clocking bits onto the link), and propagation delay (signal travel time).",
          "hint": "Two are fixed-ish, one is load-dependent, one depends on distance."
        },
        {
          "id": "performance-quic-l2-fc2",
          "type": "flashcard",
          "front": "Give the formulas for serialization and propagation delay.",
          "back": "Serialization: $d_{serial} = L / R$ (packet bits over link rate). Propagation: $d_{prop} = d / v$, with $v \\approx \\tfrac{2}{3}c \\approx 2\\times10^8$ m/s in fiber."
        },
        {
          "id": "performance-quic-l2-fc3",
          "type": "flashcard",
          "front": "State Little's Law and its queuing intuition.",
          "back": "$L = \\lambda W$: average number in system = arrival rate × average time in system. A persistently full queue means large time-in-queue (latency); you can't drain it by waiting, you must drop or slow arrivals."
        },
        {
          "id": "performance-quic-l2-fc4",
          "type": "flashcard",
          "front": "Define the bandwidth-delay product and what it physically represents.",
          "back": "$BDP = BW \\times RTT$. It is the amount of data 'in flight' that fills the path end-to-end — the pipe's volume. To fully utilize a link the in-flight window must be ≥ BDP."
        },
        {
          "id": "performance-quic-l2-fc5",
          "type": "flashcard",
          "front": "Why does TCP need window scaling on a long fat network?",
          "back": "TCP's base 16-bit receive window maxes at 64 KiB. On high-BDP paths that is far below the BDP, so the sender idles waiting for ACKs. Window scaling (RFC 7323) shifts the window field to allow much larger windows.",
          "hint": "16-bit field caps at 65,535 bytes."
        },
        {
          "id": "performance-quic-l2-fc6",
          "type": "flashcard",
          "front": "Why is propagation delay irreducible, and how does it tie to radar/RF?",
          "back": "It is set by distance over signal speed ($\\approx 2/3\\,c$ in fiber) — no faster path exists. It's the same speed-of-propagation timing that sets radar range resolution and round-trip ranging."
        },
        {
          "id": "performance-quic-l2-mc1",
          "type": "mcq",
          "prompt": "A 1 Gbps link has an 80 ms RTT. Roughly what in-flight window is needed to fully utilize it?",
          "choices": [
            "About 64 KiB",
            "About 1 MB",
            "About 10 MB",
            "About 1 GB"
          ],
          "answerIndex": 2,
          "explanation": "$BDP = 10^9\\,\\text{bits/s} \\times 0.08\\,\\text{s} = 8\\times10^7$ bits = $10^7$ bytes ≈ 10 MB. A 64 KiB window would leave the link mostly idle, hence window scaling."
        },
        {
          "id": "performance-quic-l2-mc2",
          "type": "mcq",
          "prompt": "Serialization delay for a 12,000-bit packet on a 100 Mbps link is closest to:",
          "choices": [
            "0.12 ms",
            "1.2 ms",
            "12 ms",
            "120 ms"
          ],
          "answerIndex": 0,
          "explanation": "$d_{serial} = L/R = 12000 / 10^8 = 1.2\\times10^{-4}$ s = 0.12 ms."
        },
        {
          "id": "performance-quic-l2-mc3",
          "type": "mcq",
          "prompt": "Which delay term is the one that spikes under congestion and causes load-dependent latency?",
          "choices": [
            "Propagation delay",
            "Serialization delay",
            "Queuing delay",
            "Processing delay"
          ],
          "answerIndex": 2,
          "explanation": "Queuing delay grows as buffers fill behind other traffic; the other three are roughly fixed for a given packet, distance, and link rate."
        },
        {
          "id": "performance-quic-l2-mc4",
          "type": "mcq",
          "prompt": "A server is 3,000 km away over fiber. The minimum one-way propagation delay is closest to:",
          "choices": [
            "1.5 ms",
            "15 ms",
            "150 ms",
            "1.5 s"
          ],
          "answerIndex": 1,
          "explanation": "$d_{prop} = d/v = 3\\times10^6\\,\\text{m} / (2\\times10^8\\,\\text{m/s}) = 0.015$ s = 15 ms one-way, regardless of bandwidth."
        }
      ]
    },
    {
      "id": "performance-quic-l3",
      "title": "Bufferbloat, AQM, Congestion Control & QUIC",
      "order": 3,
      "difficulty": 3,
      "content": "**Bufferbloat** is the under-appreciated latency killer: vendors oversized buffers so big that a bulk flow fills them and every other packet waits behind a queue that may hold seconds of data. Latency under load balloons even though throughput looks fine — a saturated uplink can push ping from 20 ms to 1000 ms. The fix is **Active Queue Management (AQM)**: instead of a dumb tail-drop FIFO, the queue manages itself. **CoDel** targets *sojourn time* (how long a packet sat in queue) and drops when it stays high, signaling senders to slow before the buffer bloats. **FQ-CoDel** adds fair queuing so a bulk flow can't starve an interactive one; **CAKE** bundles FQ-CoDel with shaping and per-host fairness for home routers. **ECN** (Explicit Congestion Notification) lets a router *mark* a packet (set a bit) instead of dropping it, signaling congestion without a retransmit — a cleaner feedback channel.\n\n**Congestion control** then decides the sending rate. **CUBIC** (Linux default) is *loss-based*: it grows the window along a cubic curve and treats packet loss as the congestion signal, so it tends to fill (and bloat) buffers before backing off. **BBR** is *model-based*: it probes for the bottleneck **bandwidth** and minimum **RTT** and paces to that estimate, keeping queues short. BBR can win throughput on lossy paths but raises fairness debates when sharing links with CUBIC.\n\n**QUIC** (RFC 9000) is the modern transport: a reliable, congestion-controlled, encrypted protocol built *on UDP, in user space*. It folds the transport handshake and **TLS 1.3** into one exchange (**1-RTT** setup, **0-RTT** on resumption), multiplexes independent **streams** so one lost packet doesn't head-of-line-block the others (TCP's flaw under HTTP/2), and supports **connection migration** — a connection ID survives an IP change, so a phone moving from WiFi to cellular keeps its session. User-space implementation lets transport evolve without kernel upgrades. **MPTCP** and **multipath QUIC** extend this to use several paths (WiFi + cellular) at once for aggregation or seamless failover.",
      "items": [
        {
          "id": "performance-quic-l3-fc1",
          "type": "flashcard",
          "front": "What is bufferbloat and what symptom reveals it?",
          "back": "Oversized network buffers fill with a bulk flow, so all traffic waits behind seconds of queued data. Symptom: latency under load explodes (e.g. ping jumps from 20 ms to hundreds of ms) while throughput still looks fine.",
          "hint": "Latency-under-load, not throughput."
        },
        {
          "id": "performance-quic-l3-fc2",
          "type": "flashcard",
          "front": "How does CoDel decide to drop, and what does FQ-CoDel add?",
          "back": "CoDel watches packet sojourn time (time spent in queue) and drops when it stays above target, signaling senders before the buffer bloats. FQ-CoDel adds fair queuing so a bulk flow can't starve interactive flows."
        },
        {
          "id": "performance-quic-l3-fc3",
          "type": "flashcard",
          "front": "What does ECN do differently from a normal drop?",
          "back": "ECN lets a router mark a packet (set a congestion bit) instead of dropping it. The sender learns of congestion and slows down without losing the packet or paying a retransmit."
        },
        {
          "id": "performance-quic-l3-fc4",
          "type": "flashcard",
          "front": "Contrast CUBIC and BBR congestion control.",
          "back": "CUBIC is loss-based: grows the window on a cubic curve and treats loss as the congestion signal, tending to fill buffers. BBR is model-based: estimates bottleneck bandwidth and min RTT and paces to it, keeping queues short."
        },
        {
          "id": "performance-quic-l3-fc5",
          "type": "flashcard",
          "front": "What is QUIC, in one sentence, and on what does it run?",
          "back": "QUIC (RFC 9000) is a reliable, congestion-controlled, TLS 1.3-encrypted transport with multiplexed streams, implemented in user space on top of UDP."
        },
        {
          "id": "performance-quic-l3-fc6",
          "type": "flashcard",
          "front": "What does QUIC connection migration solve, and how?",
          "back": "A QUIC connection is identified by a connection ID, not the IP/port 4-tuple, so it survives an IP change. A phone moving from WiFi to cellular keeps the same session without re-handshaking."
        },
        {
          "id": "performance-quic-l3-mc1",
          "type": "mcq",
          "prompt": "What does QUIC improve over TCP + TLS 1.3 + HTTP/2? (Pick the most complete answer.)",
          "choices": [
            "Nothing functional — it's only an encoding change",
            "Combined transport+TLS handshake (1-RTT/0-RTT), per-stream multiplexing without transport head-of-line blocking, and connection migration across IP changes",
            "It removes encryption to go faster",
            "It runs in the kernel for lower latency than TCP"
          ],
          "answerIndex": 1,
          "explanation": "HTTP/2 multiplexes streams over one TCP connection, but a single lost segment stalls all streams (transport HOL blocking), and setup needs separate TCP then TLS round trips. QUIC merges the handshake (1-RTT, 0-RTT on resume), gives independent streams so loss affects only its own stream, and migrates connections via connection ID. It runs in user space, not kernel.",
          "hint": "Think handshake RTTs, HOL blocking, mobility.",
          "image": "https://raw.githubusercontent.com/GoldCoin1987/Learning-App/main/packs/networking/images/performance-quic-1.svg"
        },
        {
          "id": "performance-quic-l3-mc2",
          "type": "mcq",
          "prompt": "Why does a loss-based controller like CUBIC tend to worsen bufferbloat?",
          "choices": [
            "It paces strictly to estimated bandwidth",
            "It keeps growing its window until a packet is dropped, which only happens after the buffer is full",
            "It marks packets with ECN instead of dropping",
            "It never retransmits"
          ],
          "answerIndex": 1,
          "explanation": "CUBIC uses loss as its signal, so it must fill the buffer (often a bloated one) before it sees a drop and backs off — maximizing queue occupancy and latency."
        },
        {
          "id": "performance-quic-l3-mc3",
          "type": "mcq",
          "prompt": "HTTP/2 over TCP still suffers head-of-line blocking because:",
          "choices": [
            "TLS encrypts the stream identifiers",
            "TCP delivers a single in-order byte stream, so one lost segment stalls every multiplexed stream until it is retransmitted",
            "HPACK header compression serializes requests",
            "UDP reorders packets"
          ],
          "answerIndex": 1,
          "explanation": "HTTP/2 multiplexes logical streams over one TCP connection, but TCP guarantees a single in-order byte stream, so a lost segment blocks delivery of all streams behind it. QUIC's per-stream delivery fixes this."
        },
        {
          "id": "performance-quic-l3-mc4",
          "type": "mcq",
          "prompt": "What advantage does implementing QUIC in user space (over UDP) provide?",
          "choices": [
            "It bypasses congestion control entirely",
            "Transport logic can evolve and deploy with the application, without waiting for OS kernel upgrades",
            "It guarantees lower propagation delay",
            "It eliminates the need for TLS"
          ],
          "answerIndex": 1,
          "explanation": "Because QUIC lives in user space atop UDP, browsers and servers ship transport improvements with app updates rather than depending on slow, fleet-wide kernel TCP-stack changes."
        },
        {
          "id": "performance-quic-l3-mc5",
          "type": "mcq",
          "prompt": "MPTCP and multipath QUIC primarily enable:",
          "choices": [
            "Encrypting the IP header",
            "Using multiple network paths (e.g. WiFi + cellular) at once for aggregation or seamless failover",
            "Removing the need for congestion control",
            "Compressing application payloads"
          ],
          "answerIndex": 1,
          "explanation": "Multipath transports spread or fail over a single logical connection across several paths/interfaces, increasing throughput or resilience — valuable on mobile devices with both WiFi and cellular."
        }
      ]
    }
  ]
}
