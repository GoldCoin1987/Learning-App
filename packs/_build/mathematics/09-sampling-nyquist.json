{
  "id": "sampling-nyquist",
  "title": "Sampling, Nyquist & Quantization",
  "order": 9,
  "lessons": [
    {
      "id": "fundamentals",
      "title": "Sampling Fundamentals & the Nyquist Theorem",
      "order": 1,
      "difficulty": 1,
      "items": [
        {
          "id": "sampling-nyquist-fundamentals-001",
          "type": "flashcard",
          "front": "What is sampling, in one sentence?",
          "back": "Sampling is the process of converting a continuous-time signal x(t) into a discrete-time sequence x[n] by recording its amplitude at uniformly spaced instants n*Ts, where Ts is the sampling period and fs = 1/Ts is the sampling rate.",
          "hint": "Continuous time -> discrete time at regular intervals."
        },
        {
          "id": "sampling-nyquist-fundamentals-002",
          "type": "flashcard",
          "front": "Mathematically, ideal (impulse) sampling is modeled as what operation on x(t)?",
          "back": "Multiplication of x(t) by a Dirac impulse train (Dirac comb) s(t) = sum over n of delta(t - n*Ts). The sampled signal is xs(t) = x(t) * s(t) = sum of x(n*Ts)*delta(t - n*Ts). This idealization captures the sample instants while ignoring real-world aperture/hold effects.",
          "hint": "Think of a comb of delta functions spaced Ts apart."
        },
        {
          "id": "sampling-nyquist-fundamentals-003",
          "type": "flashcard",
          "front": "What does multiplication by an impulse train in the time domain do to the signal's spectrum?",
          "back": "It convolves the spectrum X(f) with an impulse train in frequency (spaced at fs). The result is periodic spectral replication: copies of X(f) appear centered at every integer multiple of fs (..., -fs, 0, +fs, +2*fs, ...), each scaled by 1/Ts.",
          "hint": "Time-domain multiplication = frequency-domain convolution; the comb spacing is fs."
        },
        {
          "id": "sampling-nyquist-fundamentals-004",
          "type": "flashcard",
          "front": "State the Nyquist-Shannon sampling theorem.",
          "back": "A baseband signal containing no frequency components above B Hz is completely determined by, and can be exactly reconstructed from, samples taken at a rate fs > 2*B. Equivalently, sampling at strictly more than twice the highest frequency preserves all information.",
          "hint": "Strictly greater than twice the highest frequency."
        },
        {
          "id": "sampling-nyquist-fundamentals-005",
          "type": "flashcard",
          "front": "Distinguish the Nyquist RATE from the Nyquist FREQUENCY.",
          "back": "Nyquist RATE = 2*B: the minimum SAMPLING rate needed to capture a signal of bandwidth/highest-frequency B (a property of the SIGNAL). Nyquist FREQUENCY = fs/2: the highest signal frequency that a given sample rate fs can represent without aliasing (a property of the SAMPLER). They are easy to confuse because both involve a factor of 2.",
          "hint": "Rate is 2*B (about the signal); frequency is fs/2 (about the sampler)."
        },
        {
          "id": "sampling-nyquist-fundamentals-006",
          "type": "mcq",
          "prompt": "An audio signal has energy up to 20 kHz. What is the Nyquist rate?",
          "choices": ["10 kHz", "20 kHz", "40 kHz", "80 kHz"],
          "answerIndex": 2,
          "explanation": "The Nyquist rate is 2*B = 2 * 20 kHz = 40 kHz. This is why CD audio uses 44.1 kHz, leaving a small guard band above 40 kHz for the anti-alias filter to roll off."
        },
        {
          "id": "sampling-nyquist-fundamentals-007",
          "type": "mcq",
          "prompt": "A baseband signal is sampled at fs = 100 MHz. What is the Nyquist frequency?",
          "choices": ["25 MHz", "50 MHz", "100 MHz", "200 MHz"],
          "answerIndex": 1,
          "explanation": "The Nyquist frequency is fs/2 = 100/2 = 50 MHz. Any baseband signal content above 50 MHz will alias unless removed by an anti-alias filter before the ADC."
        },
        {
          "id": "sampling-nyquist-fundamentals-008",
          "type": "mcq",
          "prompt": "Why does the sampling theorem require fs STRICTLY greater than 2*B rather than fs equal to 2*B?",
          "choices": [
            "At exactly 2*B, adjacent spectral replicas just touch at fs/2, leaving no room for a realizable filter and risking ambiguity for components exactly at B (e.g., a sine at B can be missed if sampled at its zero crossings)",
            "Sampling hardware physically cannot run at exactly 2*B",
            "Quantization noise doubles at exactly 2*B",
            "The DFT is undefined when fs equals 2*B"
          ],
          "answerIndex": 0,
          "explanation": "At fs = 2*B the replicas meet edge-to-edge with zero guard band, so an ideal brick-wall filter would be required, and a tone exactly at B can be sampled at its zero crossings and lost. Real systems sample at fs > 2*B (oversample) to leave a transition band."
        },
        {
          "id": "sampling-nyquist-fundamentals-009",
          "type": "flashcard",
          "front": "In an ADC/embedded data acquisition chain, where does sampling actually occur and what is fs set by?",
          "back": "Sampling occurs in the sample-and-hold (track-and-hold) circuit at the ADC front end; fs is set by the ADC conversion clock (e.g., a timer-triggered conversion or a dedicated sample clock). The chosen fs together with the analog input bandwidth determines whether aliasing happens.",
          "hint": "Sample-and-hold + conversion clock."
        },
        {
          "id": "sampling-nyquist-fundamentals-010",
          "type": "mcq",
          "prompt": "After ideal sampling at rate fs, what does the spectrum of the sampled signal look like?",
          "choices": [
            "A single copy of X(f) centered at 0",
            "The baseband X(f) plus identical scaled replicas centered at every multiple of fs",
            "A flat (white) spectrum",
            "X(f) shifted up by fs/2 only"
          ],
          "answerIndex": 1,
          "explanation": "Sampling replicates the baseband spectrum periodically at intervals of fs (images at +/-fs, +/-2*fs, ...). Reconstruction works by isolating just the baseband copy with a lowpass filter; aliasing occurs when these replicas overlap."
        }
      ]
    },
    {
      "id": "aliasing",
      "title": "Aliasing, Anti-Alias Filtering & Reconstruction",
      "order": 2,
      "difficulty": 2,
      "items": [
        {
          "id": "sampling-nyquist-aliasing-001",
          "type": "flashcard",
          "front": "What is aliasing?",
          "back": "Aliasing is the irreversible corruption that occurs when a signal contains energy above the Nyquist frequency (fs/2). The spectral replicas overlap, so high-frequency content 'folds' down and masquerades as a lower frequency that is indistinguishable from a genuine low-frequency component.",
          "hint": "High frequencies impersonating low ones because replicas overlap."
        },
        {
          "id": "sampling-nyquist-aliasing-002",
          "type": "flashcard",
          "front": "Give the folding formula for the apparent (alias) frequency of a tone f sampled at fs.",
          "back": "Compute f mod fs to bring it into [0, fs). If the result is <= fs/2, that is the alias. If it is > fs/2, the alias is fs - (f mod fs). The spectrum 'folds' about each multiple of fs/2, so apparent frequency bounces between 0 and fs/2.",
          "hint": "Reduce mod fs, then reflect about fs/2 if needed."
        },
        {
          "id": "sampling-nyquist-aliasing-003",
          "type": "mcq",
          "prompt": "A 30 kHz tone is sampled at fs = 50 kHz. What apparent (alias) frequency appears in the digitized signal?",
          "choices": ["10 kHz", "20 kHz", "30 kHz", "5 kHz"],
          "answerIndex": 1,
          "explanation": "fs/2 = 25 kHz, and 30 kHz > 25 kHz so it aliases. 30 mod 50 = 30, which exceeds 25, so the alias = 50 - 30 = 20 kHz. The 30 kHz tone is indistinguishable from a real 20 kHz tone."
        },
        {
          "id": "sampling-nyquist-aliasing-004",
          "type": "mcq",
          "prompt": "A 70 kHz tone is sampled at fs = 50 kHz. What is the alias frequency?",
          "choices": ["20 kHz", "30 kHz", "10 kHz", "25 kHz"],
          "answerIndex": 0,
          "explanation": "70 mod 50 = 20 kHz, which is below fs/2 = 25 kHz, so the alias is 20 kHz directly (no further reflection needed). A tone one full fs above 20 kHz lands back at 20 kHz."
        },
        {
          "id": "sampling-nyquist-aliasing-005",
          "type": "flashcard",
          "front": "What is an anti-aliasing filter and where does it go?",
          "back": "An anti-aliasing filter is an analog lowpass filter placed BEFORE the ADC (sample-and-hold). It attenuates all energy above fs/2 (ideally above the band of interest) so that out-of-band signals and noise cannot fold into the passband. It must be analog because once sampled, aliasing is irreversible.",
          "hint": "Analog LPF before the sampler; must act before digitization."
        },
        {
          "id": "sampling-nyquist-aliasing-006",
          "type": "flashcard",
          "front": "Why does oversampling relax the anti-aliasing filter requirements?",
          "back": "Sampling well above 2*B widens the gap between the signal band edge (B) and the Nyquist frequency (fs/2). The filter's transition band can be much gentler (lower order, cheaper, less phase distortion) because anything that aliases must lie between B and fs - B, which is now far away. Oversampling trades data rate for analog filter simplicity.",
          "hint": "Bigger guard band = gentler filter roll-off."
        },
        {
          "id": "sampling-nyquist-aliasing-007",
          "type": "mcq",
          "prompt": "A radar receiver digitizes a video/IF band of 0 to 5 MHz with an ADC at fs = 12 MHz. A strong out-of-band interferer at 8 MHz leaks past a weak anti-alias filter. Where does it appear?",
          "choices": [
            "It is harmless because it is above the 5 MHz band",
            "At 4 MHz, landing inside the 0 to 5 MHz band of interest",
            "At 8 MHz, still out of band",
            "At 12 MHz, removed by the next stage"
          ],
          "answerIndex": 1,
          "explanation": "fs/2 = 6 MHz; 8 MHz > 6 MHz so it aliases. 8 mod 12 = 8 > 6, so alias = 12 - 8 = 4 MHz, which falls inside the 0-5 MHz band and corrupts the data. This is exactly why anti-alias filtering matters in receiver design."
        },
        {
          "id": "sampling-nyquist-aliasing-008",
          "type": "flashcard",
          "front": "What is ideal reconstruction (sinc interpolation)?",
          "back": "Perfect reconstruction passes the sampled signal through an ideal lowpass (brick-wall) filter with cutoff fs/2. In the time domain this convolves each sample with a sinc pulse: x(t) = sum of x[n] * sinc((t - n*Ts)/Ts). The sincs sum to exactly reproduce the band-limited original. It is ideal but non-causal/infinite, so not realizable exactly.",
          "hint": "Brick-wall LPF in frequency = sinc interpolation in time."
        },
        {
          "id": "sampling-nyquist-aliasing-009",
          "type": "mcq",
          "prompt": "A practical DAC outputs a zero-order hold (ZOH): each sample value is held constant for one period Ts. What spectral effect does ZOH introduce?",
          "choices": [
            "A flat (no) frequency response",
            "A sinc-shaped (sin(pi*f/fs)/(pi*f/fs)) amplitude droop across the band plus residual images, usually corrected by a sinc/x-compensation filter",
            "Perfect brick-wall reconstruction",
            "It removes all images automatically with no droop"
          ],
          "answerIndex": 1,
          "explanation": "ZOH convolves with a rectangular pulse of width Ts, whose transform is a sinc. This causes passband amplitude droop (about -3.9 dB at fs/2) and only partially suppresses images, so DAC chains use a sinc-compensation (inverse-sinc) filter plus an analog reconstruction lowpass."
        },
        {
          "id": "sampling-nyquist-aliasing-010",
          "type": "mcq",
          "prompt": "Which statement best explains why aliasing cannot be undone in software after the ADC?",
          "choices": [
            "Software is too slow",
            "Once overlapping replicas are summed during sampling, the aliased and genuine components occupy the same bins and are mathematically inseparable",
            "ADCs discard the sign bit",
            "Digital filters only work on real signals"
          ],
          "answerIndex": 1,
          "explanation": "Aliasing adds the folded high-frequency energy directly onto legitimate in-band content. After summation there is no information left to separate them, so prevention (analog anti-alias filtering or adequate fs) is the only cure."
        }
      ]
    },
    {
      "id": "quantization-iq",
      "title": "Quantization, SNR, Bandpass Sampling & I/Q",
      "order": 3,
      "difficulty": 3,
      "items": [
        {
          "id": "sampling-nyquist-quantization-iq-001",
          "type": "flashcard",
          "front": "What is quantization, and how many levels does an N-bit ADC provide?",
          "back": "Quantization maps the continuous sampled amplitude to the nearest of a finite set of discrete codes. An N-bit ADC has 2^N levels with step size (LSB) q = full-scale-range / 2^N. The rounding error introduced is quantization error, bounded by +/- q/2.",
          "hint": "2^N levels; step q = FSR / 2^N."
        },
        {
          "id": "sampling-nyquist-quantization-iq-002",
          "type": "flashcard",
          "front": "What is quantization noise and what is its typical model?",
          "back": "Quantization noise is the error signal between the true and quantized values. For a busy signal spanning many codes it is modeled as additive noise uniformly distributed on [-q/2, +q/2], approximately white, with variance q^2/12 (RMS = q/sqrt(12)). This model underpins the SNR-vs-bits formula.",
          "hint": "Uniform on +/- q/2; variance q^2/12."
        },
        {
          "id": "sampling-nyquist-quantization-iq-003",
          "type": "flashcard",
          "front": "State the ideal ADC SNR formula and the '6 dB per bit' rule of thumb.",
          "back": "For a full-scale sine into an ideal N-bit ADC: SNR = 6.02*N + 1.76 dB. So each added bit improves SNR by about 6.02 dB. The 1.76 dB term comes from the sine's crest factor relative to the uniform-noise model.",
          "hint": "6.02*N + 1.76 dB; ~6 dB/bit."
        },
        {
          "id": "sampling-nyquist-quantization-iq-004",
          "type": "mcq",
          "prompt": "Using SNR = 6.02*N + 1.76 dB, what is the ideal SNR of a 12-bit ADC for a full-scale sine?",
          "choices": ["72.2 dB", "74.0 dB", "62.0 dB", "98.1 dB"],
          "answerIndex": 1,
          "explanation": "6.02 * 12 + 1.76 = 72.24 + 1.76 = 74.0 dB. Each bit adds about 6 dB, so a 16-bit ADC would reach about 98.1 dB."
        },
        {
          "id": "sampling-nyquist-quantization-iq-005",
          "type": "mcq",
          "prompt": "A radar receiver must capture a 78 dB instantaneous dynamic range. Ignoring processing gain and oversampling, what is the minimum ADC bit depth from the 6.02*N + 1.76 dB rule?",
          "choices": ["10 bits", "12 bits", "13 bits", "16 bits"],
          "answerIndex": 2,
          "explanation": "Solve 6.02*N + 1.76 >= 78 -> N >= 76.24/6.02 = 12.66, so N = 13 bits gives 6.02*13 + 1.76 = 80.0 dB. 12 bits (74 dB) is insufficient; this is the core 'justify bit depth from SNR' calculation."
        },
        {
          "id": "sampling-nyquist-quantization-iq-006",
          "type": "flashcard",
          "front": "What is dither and why deliberately add noise before an ADC?",
          "back": "Dither is a small amount of noise added before quantization to decorrelate the quantization error from the signal. It breaks up harmonic/pattern distortion (turning it into a smoother noise floor) and, combined with averaging/oversampling, can recover sub-LSB resolution. Useful for low-level signals near the LSB.",
          "hint": "Add noise to randomize quantization error and kill spurs."
        },
        {
          "id": "sampling-nyquist-quantization-iq-007",
          "type": "mcq",
          "prompt": "Processing gain: oversampling by a factor OSR and then digitally filtering to bandwidth B spreads quantization noise over fs/2 but keeps signal in B. Doubling fs (OSR x2) over the same band improves SNR by approximately how much?",
          "choices": ["0 dB", "3 dB", "6 dB", "12 dB"],
          "answerIndex": 1,
          "explanation": "Quantization noise is spread roughly white over 0 to fs/2; only the fraction B/(fs/2) remains after decimation/filtering. Each 2x in fs halves the in-band noise power = +3 dB SNR (equivalent to about 0.5 extra bit, more with noise-shaping sigma-delta)."
        },
        {
          "id": "sampling-nyquist-quantization-iq-008",
          "type": "flashcard",
          "front": "What is bandpass (under)sampling and why is it used in SDR/radar?",
          "back": "Bandpass sampling deliberately samples an RF signal of bandwidth B centered at high carrier fc at a rate fs much less than 2*fc (but still > 2*B). The signal lies in a higher Nyquist zone; aliasing is used ON PURPOSE to fold the RF band down to baseband/low-IF. This eliminates an analog downconversion stage in SDR/radar receivers.",
          "hint": "Sample faster than 2*B but slower than 2*fc; use aliasing intentionally."
        },
        {
          "id": "sampling-nyquist-quantization-iq-009",
          "type": "mcq",
          "prompt": "For bandpass sampling, the key constraint (besides fs > 2*B) that must hold to avoid the band straddling a multiple of fs/2 is:",
          "choices": [
            "fs must equal fc exactly",
            "The entire band must fit within a single Nyquist zone, i.e., a valid fs satisfies 2*fc_high/n <= fs <= 2*fc_low/(n-1) for some integer n with n <= fc_high/B",
            "fs must be greater than 2*fc",
            "B must be a power of two"
          ],
          "answerIndex": 1,
          "explanation": "The band must land entirely inside one Nyquist zone (no fold across a k*fs/2 boundary). Valid rates fall in bands given by 2*fc_high/n <= fs <= 2*fc_low/(n-1), where the zone index n is bounded by floor(fc_high/B). Picking fs from a valid band folds the RF cleanly to baseband."
        },
        {
          "id": "sampling-nyquist-quantization-iq-010",
          "type": "mcq",
          "prompt": "A signal occupies 200 to 220 MHz (B = 20 MHz, fc = 210 MHz). A receiver bandpass-samples it at fs = 80 MHz. To which baseband frequency range does the 200-220 MHz band fold?",
          "choices": [
            "0 to 20 MHz",
            "20 to 40 MHz",
            "It cannot be sampled below 2*fc",
            "60 to 80 MHz"
          ],
          "answerIndex": 1,
          "explanation": "Reduce edges mod fs=80: 200 mod 80 = 40, 220 mod 80 = 60. Both fall in the 2nd half of a Nyquist zone (40-80 with fs/2=40... here zone 3: 160-200 is up-going, 200-240 is down-going), so the band reflects to 80-60=20 and 80-40=40, i.e. 20 to 40 MHz (spectrum inverted). It fits cleanly in one zone since fs=80 > 2*B=40."
        },
        {
          "id": "sampling-nyquist-quantization-iq-011",
          "type": "flashcard",
          "front": "What is I/Q sampling (complex baseband) and why is it used?",
          "back": "I/Q sampling represents a signal as a complex stream I + jQ, where I (in-phase) and Q (quadrature) are the outputs of mixing with cos and sin of the carrier (90 degrees apart). Complex baseband captures both positive and negative frequencies independently, allows asymmetric spectra, doubles usable bandwidth per real sample rate, and is the native format of SDR/radar receivers.",
          "hint": "Two channels 90 degrees apart -> complex stream that distinguishes +f from -f."
        },
        {
          "id": "sampling-nyquist-quantization-iq-012",
          "type": "mcq",
          "prompt": "For a COMPLEX (I/Q) baseband signal of one-sided bandwidth B (occupying -B/2 to +B/2), the minimum complex sample rate to avoid aliasing is:",
          "choices": ["2*B complex samples/s", "B complex samples/s", "4*B complex samples/s", "B/2 complex samples/s"],
          "answerIndex": 1,
          "explanation": "A complex signal spanning total bandwidth B needs only fs = B complex samples/s (each complex sample is two real numbers, so the real-equivalent rate still meets Nyquist). This is why SDRs quote complex sample rate equal to the channel bandwidth, not twice it."
        },
        {
          "id": "sampling-nyquist-quantization-iq-013",
          "type": "flashcard",
          "front": "Define decimation and interpolation in a multirate DSP chain.",
          "back": "Decimation reduces the sample rate by integer M: first lowpass-filter to fs/(2*M) to prevent aliasing, then keep every Mth sample (downsample). Interpolation increases the rate by integer L: insert L-1 zeros between samples (upsample), then lowpass-filter to remove the resulting images. Together (L/M) they give rational rate conversion.",
          "hint": "Decimate = filter then drop samples; interpolate = zero-stuff then filter."
        },
        {
          "id": "sampling-nyquist-quantization-iq-014",
          "type": "mcq",
          "prompt": "When decimating by M, why must the lowpass (decimation) filter come BEFORE discarding samples?",
          "choices": [
            "To save memory",
            "Because dropping samples lowers the effective fs to fs/M, so any content above fs/(2*M) would alias unless removed first",
            "Filters only work at high rates",
            "It does not matter; order is arbitrary"
          ],
          "answerIndex": 1,
          "explanation": "Downsampling by M reduces the Nyquist frequency from fs/2 to fs/(2*M). Energy between the new and old Nyquist edges must be filtered out first, otherwise it folds into the retained band. (Polyphase implementations fold the filter and the downsampler together efficiently, but logically the filtering still precedes the rate drop.)"
        }
      ]
    }
  ]
}
